import { HoverColourDirective } from './hover-colour.directive';

describe('HoverColourDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverColourDirective();
    expect(directive).toBeTruthy();
  });
});
